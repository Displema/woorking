<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Dettagli Prenotazione</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/html/Prenotazioni/VisualizzaPrenotazione.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body>

<!-- NAVBAR -->
{% include 'navbar/navbar.html.twig' %}

<!-- CONTENUTO PRINCIPALE -->
<div class="container my-5 dettagli-container">
    <div class="row g-4">

        <!-- Galleria Immagini -->
        {% for reservation in reservations %}
        <div class="col-lg-6">
            <div class="galleria-foto d-flex flex-wrap gap-3">
                {% for photo in reservation.photo %}
                <img src="{{ photo }}" class="img-fluid rounded" alt="Foto 1">
                {%endfor %}
            </div>
        </div>

        <!-- Info Ufficio -->
        <div class="col-lg-6">
            <div class="info-ufficio">
                <h1 class="mb-3">{{ reservation.office.titolo }}</h1>
                <p><strong>Indirizzo: </strong> {{ reservation.office.indirizzo }}</p>
                <p><strong>Posti disponibili: </strong>{{reservation.office.numeroPostazioni}}</p>
                <p><strong>Contatti: </strong> info@affittaufficio.it - +39 0123 456789</p>
                <p><strong>Descrizione: </strong> {{ reservation.office.descrizione }}</p>

                <hr>
                <p><strong>Prenotato il:</strong> <span class="text-primary">{{ reservation.reservation.data|date('d/m/Y')  }}</span> per la fascia della <span class="text-primary">{{ prenotation.prenotazione.fascia }}</span> </p>
                <p><strong>Prenotazione effettuata il:</strong> <span class="text-success">12/04/2026</span> alle ore <span class="text-success">08:40</span></p>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<!-- FOOTER -->
{% include 'Footer/footer.html.twig' %}

<!-- SCRIPT -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
