<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Dettagli Prenotazione</title>
    <!-- link to bootstrap for responsive template -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- link to the css for the style -->
    <link rel="stylesheet" href="/html/User/Prenotazioni/VisualizzaPrenotazione.css">
    <!-- link to awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>

<!-- include navbar -->


    {% include 'navbar/navbar.html.twig' %}



<!-- main content -->
<div class="container my-5 dettagli-container">
    <div class="row g-4">

        <!-- photo gallery -->

        <div class="col-lg-6">
            <div class="galleria-foto d-flex flex-wrap gap-3">
                {% for photo in reservation.ufficio.foto %}
                <img src="/static/img/{{ photo.id }}" class="img-fluid rounded" alt="Foto 1">
                {%endfor %}
            </div>
        </div>

        <!--  office information -->
        <div class="col-lg-6">
            <div class="info-ufficio">
                <h1 class="mb-3">{{ reservation.ufficio.titolo }}</h1>
                <p><strong>Indirizzo: </strong> {{ reservation.ufficio.indirizzo }}</p>
                <p><strong>Posti disponibili: </strong>{{reservation.ufficio.numeroPostazioni}}</p>
                <p><strong>Contatti: </strong> info@affittaufficio.it - +39 0123 456789</p>
                <p><strong>Descrizione: </strong> {{ reservation.ufficio.descrizione }}</p>

                <hr>
                <p><strong>Prenotazione per il:</strong> <span class="text-primary">{{ reservation.data|date('d/m/Y')  }}</span> per la fascia <span class="text-primary">{{ reservation.fascia.value }}</span> </p>
                <p><strong>Prenotazione effettuata il:</strong> <span class="text-success">12/04/2026</span> alle ore <span class="text-success">08:40</span></p>
            </div>
        </div>

    </div>
</div>

<!-- include footer -->
{% include 'Footer/footer.html.twig' %}

</body>
</html>
