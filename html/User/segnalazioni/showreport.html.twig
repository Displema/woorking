<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8" />
    <title>Le tue segnalazioni per questo ufficio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />

    <style>
        html, body {
            background-color: #222;
            color: #eee;
            min-height: 100vh;
        }

        /* Contenitore principale più stretto e centrato */
        .container {
            max-width: 720px;
        }

        /* Titolo principale */
        h2.text-center {
            font-weight: 700;
            letter-spacing: 1.1px;
            text-transform: uppercase;
        }

        /* Box segnalazioni */
        .report-card {
            background: #2a2a2a;
            border-radius: 12px;
            box-shadow: 0 4px 12px rgb(0 0 0 / 0.4);
            padding: 1.5rem 2rem;
            transition: background-color 0.3s ease;
        }
        .report-card:hover {
            background-color: #3c3c3c;
        }

        /* Icona commento */
        .report-card i.fas.fa-comment-dots {
            color: #f03e3e;
            font-size: 1.25rem;
        }

        /* Badge stato */
        .badge-active {
            background-color: #ffc107;
            color: #212529;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            padding: 0.4em 0.9em;
            border-radius: 20px;
        }
        .badge-solved {
            background-color: #198754;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            padding: 0.4em 0.9em;
            border-radius: 20px;
        }

        /* Messaggio vuoto più grande e moderno */
        .alert-warning {
            background-color: #333 !important;
            color: #ffc107 !important;
            border: none;
            border-radius: 14px;
            box-shadow: 0 0 20px #ffc107aa;
            font-size: 1.3rem;
            font-weight: 600;
        }

        .alert-warning h2 {
            font-size: 2rem;
        }

        /* Bottone personalizzato */
        .btn-primary {
            background: #0d6efd;
            border: none;
            padding: 0.6rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgb(13 110 253 / 0.4);
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #084298;
            box-shadow: 0 8px 20px rgb(8 66 152 / 0.6);
        }

        /* Info ufficio */
        .form-container h2 {
            font-weight: 700;
            color: #0dcaf0;
        }
        .form-container p {
            color: #bbb;
            font-size: 1rem;
            max-width: 540px;
            margin: 0 auto;
        }
    </style>
</head>
<body>

<!-- Navbar -->
{% include 'navbar/navbar.html.twig' %}

<div class="container mt-5 mb-5">
    <!-- Titolo della pagina -->
    <h2 class="text-center mb-4">
        <i class="fas fa-exclamation-triangle me-2"></i> Le tue segnalazioni per questo ufficio
    </h2>

    <div class="form-container mb-5 text-center">
        <h2>{{ office.titolo }}</h2>
        <p>
            Benvenuto nella sezione dedicata alle segnalazioni fatte da te per questo ufficio. Qui puoi leggere le segnalazioni lasciate da te.
        </p>
    </div>

    <!-- Elenco segnalazioni -->
    {% if reports|length > 0 %}
        {% for report in reports %}
            <div class="report-card mb-4 d-flex justify-content-between align-items-center">
                <div>
                    <i class="fas fa-comment-dots me-2"></i>{{ report.commento }}
                </div>
                {% if report.state == 'Active' %}
                    <span class="badge badge-active">Active</span>
                {% else %}
                    <span class="badge badge-solved">Solved</span>
                {% endif %}
            </div>
        {% endfor %}
    {% else %}
        <div class="alert alert-warning text-center py-5">
            <h2 class="mb-3">
                <i class="fas fa-exclamation-triangle me-2"></i> Nessuna segnalazione trovata
            </h2>
            <p>Non hai ancora effettuato segnalazioni per questo ufficio.</p>
            <a href="/offices/{{office.id}}/report" class="btn btn-primary mt-3">
                <i class="fas fa-flag me-1"></i> Compila la tua prima segnalazione
            </a>
        </div>
    {% endif %}
</div>

<!-- Footer -->
{% include 'Footer/footer.html.twig' %}

<!-- Bootstrap JS Bundle (optional if you use Bootstrap JS components) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
